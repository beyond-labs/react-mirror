# Counter Example

## Run locally

```sh
git clone https://github.com/ashtonwar/react-mirror.git
cd react-mirror
yarn install
cd examples/counter
yarn install
yarn start
```

Then open `http://localhost:3000/`.
