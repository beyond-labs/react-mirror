React Mirror Examples
=====================

```sh
git clone https://github.com/beyond-labs/react-mirror.git
cd react-mirror
npm install
cd examples
yarn install
yarn start
```

Then open `http://localhost:9000`.
